{
  "name": "api",
  "private" : true,
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch . index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.1.4",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
